import { params } from './meta/LayoutFlow.params';
import { sampleData } from './meta/LayoutFlow.sample';
import { dimensions } from './meta/LayoutFlow.dimensions';
import { screenshots } from './meta/LayoutFlow.screenshots';
import { IBlockInfo } from '@gdi/web-ui';

export const blockInfo: IBlockInfo = {
    id: 'com.useGdi.templates.gdi.layout-flow',
    params,
    sampleData,
    dimensions,
    screenshots,
    tags: ['type-layout'],
};
