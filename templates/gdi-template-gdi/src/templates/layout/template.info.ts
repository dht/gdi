import { widgets } from './blocks';

export const templateInfo: ITemplateInfo = {
    id: 'com.usegdi.templates.layout',
    version: '',
    author: {
        name: '',
    },
    widgetsCount: Object.keys(widgets).length,
};
