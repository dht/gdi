# Running a local GDI instance

## Requirements

- A valid OpenAI API key

## Setup

Update the `.env` file with your OpenAI API key

## Running

Run the following command to start the GDI instance:

```bash
gdi start
```
