Session 31-08-2022, 21:45

tries to install with
npm i -D @gdi/cli
npx gdi rebuild
npx gdi create site my-site
require node v16+

install gdi as dev dependency inside gdi-admin
add prerequisites
perhaps use firebase
change npx
ask about the firebase credentials one by one

add step about firebase-cli installation

notes:

-   specify firebase app creation stages
-   specify where to get the DATABASE_URL and MEASUREMENT_ID

npx gdi seed

npx gdi seed says
firebaselite

add stage of enabling firestore
wait a moment for firestore rules to apply

npm run dev => should be gdi start

preview

1920x887

multiple tags

contain the firebase
proxy for firebase

gdi create project
gdi start

gdi deploy

command not found typescript

@types/sharp
