import { site } from '@gdi/store-site';

export const actions = site.actions;
export const selectors = site.selectors;
export const reducers = site.reducers;
export const initialState = site.initialState;
export const endpointsConfig = site.endpointsConfigOverrides;
export const clearState = site.clearState;
