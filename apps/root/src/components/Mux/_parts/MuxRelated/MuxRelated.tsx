import React from 'react';
import { Wrapper } from './MuxRelated.style';

export type MuxRelatedProps = {};

export function MuxRelated(_props: MuxRelatedProps) {
    return (
        <Wrapper className="MuxRelated-wrapper" data-testid="MuxRelated-wrapper">
            MuxRelated
        </Wrapper>
    );
}

export default MuxRelated;
