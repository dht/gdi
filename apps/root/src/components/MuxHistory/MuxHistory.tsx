import React from 'react';
import { Wrapper } from './MuxHistory.style';

export type MuxHistoryProps = {};

export function MuxHistory(_props: MuxHistoryProps) {
    return (
        <Wrapper className="MuxHistory-wrapper" data-testid="MuxHistory-wrapper">
            MuxHistory
        </Wrapper>
    );
}

export default MuxHistory;
