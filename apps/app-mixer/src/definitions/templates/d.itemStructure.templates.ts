export const itemStructure = `export type ITemplate = {
    meta: IMetaSite;
    locale: ILocale;
    pages: IPages;
    pageInstances: IPageInstances;
    instances: IWidgetInstances;
    instancesProps: Json;
    widgets: IWidgets;
    images: IImages;
    palette: IPalette;
    fonts: IFonts;
    breakpoints: IBreakpoints;
    datasets: IDatasets;
};
`;
