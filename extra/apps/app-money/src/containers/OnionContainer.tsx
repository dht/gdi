import React from 'react';
import Onion from '../components/Onion/Onion';
import { useSelector, useDispatch } from 'react-redux';
import { selectors } from '../store';

export const OnionContainer = () => {
    return <Onion />;
};
