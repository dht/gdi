import styled from 'styled-components';

export const Wrapper = styled.div`
    display: flex;
    flex-direction: row;
    align-items: baseline;
    position: absolute;
    left: 100px;
    top: 10px;

    img {
        height: 80px;
    }
`;
