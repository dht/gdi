import { IBlockParams } from '@gdi/web-ui';
import { id } from '../Footer';

export const params: IBlockParams = {
    id,
    schema: {
        strings: {
            copyright: 'text',
        },
        colors: {},
        extra: {},
    },
};
