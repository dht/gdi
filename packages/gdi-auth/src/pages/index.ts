import { LoginPageContainer } from './login/LoginPage.container';
import { LogoutPageContainer } from './logout/LogoutPage.container';

export const pages = {
  login: LoginPageContainer,
  logout: LogoutPageContainer,
};
