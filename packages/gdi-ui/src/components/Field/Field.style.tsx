import styled from 'styled-components';

export const Wrapper = styled.div`
  flex: 1;
  margin: 20px 0;

  &.noMargin {
    margin: 0;
    margin-top: -16px;
  }
`;

export const Editor = styled.div`
  height: 170px;
  display: flex;
`;
