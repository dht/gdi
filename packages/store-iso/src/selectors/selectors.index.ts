import * as animation from './selectors.animation';
import * as base from './selectors.base';
import * as dots from './selectors.dots';
import * as options from './selectors.options';
import * as raw from './selectors.raw';
import * as singles from './selectors.singles';
import * as wave from './selectors.wave';

export const selectors = {
  raw,
  animation,
  base,
  options,
  singles,
  wave,
  dots,
};
